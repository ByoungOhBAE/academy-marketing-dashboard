# 📢 학원 홍보 마케팅 워크플로우 (Absolute Rules v1.8)

이 문서는 학원 홍보 콘텐츠 생성의 **변하지 않는 절대 규칙**을 정의합니다.
작업 수행 시 반드시 이 규칙을 따르십시오.

---

## 0. 필수 참고 자료
작업을 시작하기 전, 반드시 아래 파일들을 읽으세요:
- **ACADEMY_INFO.md** — 학원 기본 정보, 타겟 고객, 검색 키워드, 콘텐츠 제작 가이드
- **PERSONAS.md** — 버전별(A/B/C) 페르소나 상세 정의 (블로그/인스타 각각 구분)
- **data.json** — 기존 콘텐츠 구조 파악 (블로그/인스타 버전 분리 확인)

이 자료들이 글의 **타겟, 키워드, 톤앤매너**를 결정합니다.

---

## 1. 폴더 구조 및 준비 (Input)
모든 작업은 아래 경로를 기준으로 수행합니다.
- **메인 경로:** `C:\Users\tahar\clawd\academy_marketing`
  - `📂 1_블로그용`
  - `📂 2_인스타용`

### 👁️ 실시간 스캔 원칙 (Real-time Scan)
- **[절대 규칙]** 작업을 시작하기 전, 반드시 터미널 명령어나 스크립트를 사용하여 **현재 폴더 상태를 실시간으로 확인**해야 합니다.

### 📸 사진 복사 원칙 (Image Copy)
- 외부 사진은 반드시 작업 폴더(`YYMMDD_주제`)로 복사 후 사용합니다.

### 🏷️ 태그 규칙
- **작업 시작 전:** 폴더명 앞에 **`[작업대기]`**를 붙여야 합니다.

---

## 2. 생성 및 완료 (Atomic Process)

### A. 스캔
- `[작업대기]` 태그가 붙은 폴더만 작업합니다.

### B. 콘텐츠 생성 (경로 예측)
- 블로그/인스타 각 채널에 맞는 3가지 버전(A/B/C) + Footer 포함 원고 작성.
- JSON 데이터의 이미지 경로는 **미래에 변경될 경로(`[작성완료]`)를 미리 예측하여 작성**합니다.


#### 📸 이미지 활용 및 글 분량 규칙 (블로그 전용)
- **[최대 활용 원칙]** 폴더에 있는 사진을 주제와 내용에 어울리는 한 **최대한 많이 사용**합니다.
- **[선택적 제외]** 주제/내용과 어울리지 않을 경우 사용하지 않는 것을 허용합니다.
- **[분량 확대 원칙]** 블로그 글은 단순 요약(200자 이하)으로 작성하지 마십시오. **최소 800자~1,500자 이상의 풍부한 분량**으로 작성해야 합니다. 폴더에 있는 사진 개수가 많을수록, 각 사진에 대한 상세한 묘사를 더해 글의 전체 길이를 늘리세요.
- **[본문 내 이미지 교차 배치 (매우 중요)]** 사진을 글의 맨 앞이나 끝에 몰아넣지 마십시오. **반드시 `content` 본문 텍스트 작성 시, 글의 흐름(도입-조리과정-완성-결론 등)에 맞춰 `<img src="예측된_이미지_경로">` 태그를 문단과 문단 사이에 직접 삽입**하여 텍스트와 사진이 자연스럽게 교차되도록 만드세요.
- **[사진 기반 스토리텔링]** 이미지가 삽입된 위치 전후에는 반드시 해당 사진 속 상황(신선한 식재료의 상태, 기능장님의 시연 과정, 수강생들의 열정적인 분위기, 완성작의 디테일한 맛 묘사 등)을 구체적으로 스토리텔링하여 글을 풍성하게 만드십시오.
- **[최대 활용 원칙]** 폴더에 있는 사진을 주제와 내용에 어울리는 한 **최대한 많이 사용**합니다. (단, 어울리지 않는 사진은 제외 가능)

#### 👥 버전별 페르소나 (A/B/C)
**[필수 참고]** 페르소나 상세 정의는 반드시 **`PERSONAS.md`** 를 읽고 따르세요.

**블로그용 페르소나 요약:**
- **A 버전:** 마케팅 카피라이터 — 설득력 있는 구조, SEO 키워드 자연스럽게 삽입, 명확한 CTA
- **B 버전:** 부산 살림 블로거 — 주부·경력단절 공감, 따뜻한 구어체, 개인 경험 스토리텔링
- **C 버전:** 학원 공식 블로그 담당 — 정확한 정보 전달, 커리큘럼·위치·문의처 중심

**인스타용 페르소나 요약:**
- **A 버전:** 트렌디 푸드 인플루언서 — 짧고 임팩트, MZ 감성, 비주얼 강조, 저장·공유 유도
- **B 버전:** 따뜻한 일상 계정 — 감성적 공감, 일상 느낌, 소소한 성취감
- **C 버전:** 학원 공식 인스타 계정 — 합격/성과/안내, 명확한 정보 + 문의 유도

#### #️⃣ 해시태그 규칙
- **[필수]** Footer 밑에 글 주제와 내용에 어울리는 **#태그를 여러 개** 추천하여 적습니다.
- 지역, 카테고리, 키워드 등을 고려하여 검색 최적화에 도움이 되는 태그를 선정합니다.

### C. 데이터 저장
- **[절대 경로]** `C:\Users\tahar\clawd\marketing_dashboard\public\data.json`
- **[데이터 보존 원칙]** 반드시 기존 `data.json` 파일을 먼저 읽고 그 구조(Schema)를 분석해야 합니다.
- **[채널별 분리 저장]**
  - `1_블로그용` 폴더의 작업물 ➔ `data.blog` 배열에만 추가
  - `2_인스타용` 폴더의 작업물 ➔ `data.insta` 배열에만 추가
- **[안전한 병합]** 기존 데이터 구조를 절대 훼손하지 않고, 분석된 형식에 맞춰 가공된 새 데이터를 **맨 앞에 추가(Append/Unshift)**합니다.
- 데이터 구조 예시:
  ```json
  {
    "blog": [ ... ],
    "insta": [ ... ]
  }
  ```

### D. 폴더 이동 및 경로 검수 (Critical Step)

**[핵심 구조]**
- **블로그용 원본:** `C:\Users\tahar\clawd\academy_marketing\1_블로그용\`
- **블로그용 최종:** `C:\Users\tahar\clawd\marketing_dashboard\public\photos\1_블로그용\`
- **인스타용 원본:** `C:\Users\tahar\clawd\academy_marketing\2_인스타용\`
- **인스타용 최종:** `C:\Users\tahar\clawd\marketing_dashboard\public\photos\2_인스타용\`

**[중요]** 블로그용과 인스타용은 **완전히 독립적**이며, 서로 교차하지 않습니다!

#### 1️⃣ 폴더 이동/배포
각 채널별로 **자신의 원본 폴더만** photos 폴더로 옮깁니다:

**블로그용:**
```
academy_marketing/1_블로그용/[작성완료] 260207_조리기능장_수업/
                                            ↓
marketing_dashboard/public/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/
```

**인스타용:**
```
academy_marketing/2_인스타용/[작성완료] 260207_조리기능장수업/
                                    ↓
marketing_dashboard/public/photos/2_인스타용/[작성완료] 260207_조리기능장수업/
```

#### 2️⃣ **경로 매칭 검수 (필수!)**

**블로그 항목의 경로:**
```json
{
  "id": "260207_조리기능장_수업",
  "thumbnail": "/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/...",
  "content": {
    "A": [
      {
        "title": "...",
        "images": [
          "/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/image1.jpg",
          "/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/image2.jpg"
        ]
      }
    ]
  }
}
```
⚠️ **모든 경로가 `/photos/1_블로그용/` 시작!**

**인스타 항목의 경로:**
```json
{
  "id": "260207_조리기능장수업",
  "thumbnail": "/photos/2_인스타용/[작성완료] 260207_조리기능장수업/...",
  "content": {
    "A": { "text": "..." }
  }
}
```
⚠️ **경로가 `/photos/2_인스타용/` 시작!**

#### 3️⃣ 검수 체크리스트

작업 완료 전 반드시 아래를 확인하세요:

**폴더 검수:**
- [ ] **블로그 폴더 존재:** `marketing_dashboard/public/photos/1_블로그용/[작성완료] YYMMDD_주제/`
- [ ] **인스타 폴더 존재:** `marketing_dashboard/public/photos/2_인스타용/[작성완료] YYMMDD_주제/`

**data.json 검수:**
- [ ] **블로그 항목의 모든 경로:** `/photos/1_블로그용/` 포함
  - `thumbnail` ✅
  - `content.A[0].images[]` 의 모든 경로 ✅
- [ ] **인스타 항목의 모든 경로:** `/photos/2_인스타용/` 포함
  - `thumbnail` ✅
- [ ] **폴더명-ID 일치:**
  - 블로그: `id: "260207_조리기능장_수업"` ↔️ 폴더: `[작성완료] 260207_조리기능장_수업`
  - 인스타: `id: "260207_조리기능장수업"` ↔️ 폴더: `[작성완료] 260207_조리기능장수업`

#### 4️⃣ 경로 오류 패턴 (절대 금지!)

**❌ 잘못된 예시:**
```json
// 블로그 항목인데 2_인스타용 경로
{
  "blog": [{ "thumbnail": "/photos/2_인스타용/..." }]
}

// 인스타 항목인데 1_블로그용 경로
{
  "insta": [{ "thumbnail": "/photos/1_블로그용/..." }]
}
```

**✅ 올바른 예시:**
```json
// 블로그는 항상 1_블로그용
{ "blog": [{ "thumbnail": "/photos/1_블로그용/..." }] }

// 인스타는 항상 2_인스타용
{ "insta": [{ "thumbnail": "/photos/2_인스타용/..." }] }
```

### E. 최종 완료 처리 (각 채널별 독립 확인)

#### 블로그용 완료 체크리스트
1. [ ] **경로 검증:** data.json 블로그 항목의 모든 경로 = `/photos/1_블로그용/...`
2. [ ] **브라우저 테스트:** localhost:3000 → 블로그 버튼 → 이미지+텍스트 OK
3. [ ] **폴더 확인:** `photos/1_블로그용/[작성완료] YYMMDD_주제/` 존재
4. [ ] **data.json 저장**
5. [ ] **폴더명 완료 표시:** academy_marketing/1_블로그용 폴더명 `[작업대기]` → `[작성완료]`

#### 인스타용 완료 체크리스트
1. [ ] **경로 검증:** data.json 인스타 항목의 모든 경로 = `/photos/2_인스타용/...`
2. [ ] **브라우저 테스트:** localhost:3000 → 인스타 버튼 → 이미지+텍스트 OK
3. [ ] **폴더 확인:** `photos/2_인스타용/[작성완료] YYMMDD_주제/` 존재
4. [ ] **data.json 저장**
5. [ ] **폴더명 완료 표시:** academy_marketing/2_인스타용 폴더명 `[작업대기]` → `[작성완료]`

#### ⚠️ 최종 체크 (필수!)
- [ ] 브라우저 **새로고침** (F5 또는 Ctrl+Shift+Del)
- [ ] 블로그 모드: A/B/C 옵션 전환 → 각각 다른 내용 표시 ✅
- [ ] 인스타 모드: A/B/C 옵션 전환 → 각각 다른 내용 표시 ✅
- [ ] 개발자도구(F12) → Console에 404 에러 없음 ✅

**✅ 양쪽 모두 통과하면 완료!**

---

## 2.5 ⚠️ 대시보드 데이터 구조 (중요 버그 픽스 가이드)

### 블로그 vs 인스타 데이터 구조 차이
**[중요]** `data.json`의 블로그와 인스타는 `content` 필드의 구조가 다릅니다.

#### 📝 블로그 구조
```json
{
  "blog": [
    {
      "id": "260207_조리기능장_수업",
      "content": {
        "A": [ { "title": "...", "content": "...", "images": [...] } ],  // ⚠️ 배열!
        "B": [ { ... } ],
        "C": [ { ... } ]
      }
    }
  ]
}
```

#### 📸 인스타 구조
```json
{
  "insta": [
    {
      "id": "260207_조리기능장수업",
      "thumbnail": "/photos/2_인스타용/...",
      "content": {
        "A": { "text": "...", "audit": {...} },  // ⚠️ 객체!
        "B": { "text": "...", "audit": {...} },
        "C": { "text": "...", "audit": {...} }
      }
    }
  ]
}
```

### 핵심 규칙
1. **블로그:** `content.A`는 배열 → `content.A[0]`으로 첫 번째 요소 접근
2. **인스타:** `content.A`는 객체 → `content.A`로 직접 접근
3. **이미지 경로:** 블로그는 `content` 내 각 포스트에 `images` 배열 포함, 인스타는 아이템 레벨의 `thumbnail` 사용

### 이미지 폴더 준비 체크리스트
- ✅ `C:\Users\tahar\clawd\marketing_dashboard\public\photos\1_블로그용` 존재 확인
- ✅ `C:\Users\tahar\clawd\marketing_dashboard\public\photos\2_인스타용` 존재 확인
  - **[주의]** 인스타 이미지 폴더가 없으면 대시보드에서 사진이 엑박(broken image)으로 표시됩니다
  - **[해결]** `1_블로그용` 폴더의 관련 하위 디렉토리를 `2_인스타용`으로 복사

### 필드명 매핑 규칙
| 필드 | 블로그 | 인스타 | 설명 |
|------|--------|--------|------|
| **제목** | `post.title` | 없음 | 블로그만 제목 필드 있음 |
| **본문** | `post.content` | `post.text` | 인스타는 `text`, 블로그는 `content` |
| **이미지** | `item.content.images` (배열) | `item.thumbnail` | 인스타는 썸네일 1개, 블로그는 복수 |
| **메타** | `post.audit` | `post.audit` | 페르소나/키워드/톤 정보 (동일) |

**[주의]** 블로그의 `post.images`는 본문 내 `<img src>` 태그로도 렌더링되므로 중복 표시되지 않도록 주의!

### HTML 렌더링 로직 (index.html)
```javascript
if (currentMode === 'blog') {
  // Blog: content[option]은 배열
  const posts = item.content[currentOption] || [];
  if (posts.length > 0) {
    post = posts[0];  // ← 배열에서 첫 요소 추출
  }
  // 렌더링: post.title, post.content (HTML 포함)
} else {
  // Insta: content[option]은 객체
  post = item.content[currentOption] || null;  // ← 직접 객체 접근
  // 렌더링: post.text (줄바꿈을 <br>로 변환)
  // 이미지: item.thumbnail 사용
}
```

---

## 2.6 ✅ 마무리 검증 가이드 (Quick Checklist)

**[원칙]** 블로그용과 인스타용은 **완전히 독립적**이며, 교차하지 않습니다!

### 블로그용 검증 체크 (10초)
- [ ] 폴더 확인: `photos/1_블로그용/[작성완료] 260207_조리기능장_수업/` 존재
- [ ] data.json (blog 배열):
  - `id`: "260207_조리기능장_수업"
  - `thumbnail`: "/photos/1_블로그용/..." ✅
  - 모든 이미지 경로가 "/photos/1_블로그용/..." ✅
- [ ] 브라우저: localhost:3000 → 블로그 버튼 → 이미지+텍스트 표시 ✅

### 인스타용 검증 체크 (10초)
- [ ] 폴더 확인: `photos/2_인스타용/[작성완료] 260207_조리기능장수업/` 존재
- [ ] data.json (insta 배열):
  - `id`: "260207_조리기능장수업"
  - `thumbnail`: "/photos/2_인스타용/..." ✅
- [ ] 브라우저: localhost:3000 → 인스타 버튼 → 이미지+텍스트 표시 ✅

### 🚨 금지된 패턴
- ❌ 블로그에 `/photos/2_인스타용/` 경로 있음?
- ❌ 인스타에 `/photos/1_블로그용/` 경로 있음?

**위 둘 다 "아니오"여야 정상!**

---

## 3. 출력 규칙 (Output)
- 웹 대시보드(`http://localhost:3000`)
- `index.html` (Single Page)

---

## 4. 고정 정보 (Footer Template)

**[블로그용]**
```html
<hr>
<div style="background:#f9f9f9; padding:20px; border-radius:10px; border:1px solid #eee;">
    <h3 style="margin-top:0;">🍳 한국조리기능장요리발효학원</h3>
    <p><b>문의:</b> 051-544-5380 / 010-3688-5380</p>
    <p><b>위치:</b> 부산 해운대구 반송로 924번길 6-2, 2~3층 (영산대역 4번 출구)</p>
    <p><b>운영:</b> 평일 09:00~21:00 (주말 상담 예약 필수)</p>
    <p>👉 <a href="https://naver.me/x1mx2Gwq" target="_blank">네이버 지도 보기</a></p>
</div>
```

**[인스타용]**
```text
───────────────
👨‍🍳 한국조리기능장요리발효학원
📍 부산 해운대구 반송로 924번길 6-2
📞 051-544-5380
💌 문의 상담 환영
───────────────
#부산요리학원 #해운대요리학원 #반송동 #조리기능장 #요리자격증
```

---

## 5. 🔧 데이터 추가 및 대시보드 배포 체크리스트

새로운 콘텐츠를 `data.json`에 추가한 후 **반드시 다음을 확인**하세요:

### A. 폴더 작업 (각각 독립적)

**[중요]** 블로그용과 인스타용은 **교차하지 않습니다**. 각각 독립적으로 진행합니다.

#### 블로그용 작업 (academy_marketing → photos/1_블로그용)
- [ ] **원본 작성:** `C:\Users\tahar\clawd\academy_marketing\1_블로그용\[작성완료]YYMMDD_주제\`
  - 이미지 저장
  - data.json에 블로그 항목 추가 (경로: `/photos/1_블로그용/...`)
- [ ] **최종 배포:** 폴더를 아래 경로로 이동/복사
  ```
  → C:\Users\tahar\clawd\marketing_dashboard\public\photos\1_블로그용\[작성완료]YYMMDD_주제\
  ```

#### 인스타용 작업 (academy_marketing → photos/2_인스타용)
- [ ] **원본 작성:** `C:\Users\tahar\clawd\academy_marketing\2_인스타용\[작성완료]YYMMDD_주제\`
  - 이미지 저장
  - data.json에 인스타 항목 추가 (경로: `/photos/2_인스타용/...`)
- [ ] **최종 배포:** 폴더를 아래 경로로 이동/복사
  ```
  → C:\Users\tahar\clawd\marketing_dashboard\public\photos\2_인스타용\[작성완료]YYMMDD_주제\
  ```

**[참고]** 블로그용과 인스타용 폴더는 별도로 관리되며, 각각의 내용/이미지도 독립적입니다.

### B. data.json 검증 (경로 매칭 중심)

#### 📋 구조 검증
- [ ] **블로그 데이터 구조:** `content.A`, `content.B`, `content.C` 각각이 **배열**이어야 함
  ```json
  "content": {
    "A": [{ "title": "...", "content": "...", "images": [...] }],
    "B": [{ ... }],
    "C": [{ ... }]
  }
  ```
- [ ] **인스타 데이터 구조:** `content.A`, `content.B`, `content.C` 각각이 **객체**여야 함
  ```json
  "content": {
    "A": { "text": "...", "audit": {...} },
    "B": { "text": "...", "audit": {...} },
    "C": { "text": "...", "audit": {...} }
  }
  ```

#### 🔍 경로 분리 확인 (CRITICAL!)
**[필수]** 블로그는 항상 `1_블로그용`, 인스타는 항상 `2_인스타용` 경로만 사용!

- [ ] **블로그 항목 — 모든 경로가 `/photos/1_블로그용/` 시작**
  ```json
  {
    "blog": [
      {
        "id": "260207_조리기능장_수업",
        "thumbnail": "/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/...",
        "content": {
          "A": [{ 
            "images": [
              "/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/image1.jpg",
              "/photos/1_블로그용/[작성완료] 260207_조리기능장_수업/image2.jpg"
            ] 
          }]
        }
      }
    ]
  }
  ```
  ✅ 모든 경로가 `1_블로그용` 포함

- [ ] **인스타 항목 — 모든 경로가 `/photos/2_인스타용/` 시작**
  ```json
  {
    "insta": [
      {
        "id": "260207_조리기능장수업",
        "thumbnail": "/photos/2_인스타용/[작성완료] 260207_조리기능장수업/..."
      }
    ]
  }
  ```
  ✅ 경로가 `2_인스타용` 포함

#### ❌ 절대 금지 패턴
```json
// ❌ 오류 1: 블로그인데 2_인스타용 경로
{ "blog": [{ "thumbnail": "/photos/2_인스타용/..." }] }

// ❌ 오류 2: 인스타인데 1_블로그용 경로
{ "insta": [{ "thumbnail": "/photos/1_블로그용/..." }] }
```

#### 🏷️ 폴더명-ID 일치 확인
- [ ] 각 항목의 `id` 값이 실제 폴더명과 일치
  ```
  id: "260207_조리기능장_수업"
  ↓
  폴더: [작성완료] 260207_조리기능장수업 ✅
  
  id: "260204_단체급식수업"
  ↓
  폴더: [작성완료] 260204_단체급식수업 ✅
  ```
  
- [ ] **공백 확인:** 일부 폴더명이 `[작성완료]` 뒤에 공백이 있는지 확인
  ```
  ❌ [작성완료]단체급식수업 (공백 없음)
  ✅ [작성완료] 260207_조리기능장수업 (공백 있음)
  ```
  경로에도 동일하게 반영되어야 함

### C. 대시보드 테스트 (경로 검증 중심)
1. **로컬 서버 실행**
   ```bash
   cd C:\Users\tahar\clawd\marketing_dashboard
   npm start  # 또는 node server.js
   ```
2. **브라우저 접속:** http://localhost:3000

3. **블로그 모드 테스트**
   - [ ] 네이버 블로그 버튼 클릭
   - [ ] 콘텐츠 + 이미지가 모두 표시되는지 확인
   - [ ] 이미지 소스를 개발자 도구(F12)에서 확인
     - 모든 이미지 경로가 `/photos/1_블로그용/...` 포함 확인
   - [ ] A/B/C 옵션 전환 시 각각 다른 버전 표시 확인

4. **인스타 모드 테스트**
   - [ ] 인스타그램 버튼 클릭
   - [ ] 썸네일 이미지가 표시되는지 확인 (엑박 없음!)
   - [ ] 개발자 도구에서 이미지 경로 확인
     - 이미지 경로가 `/photos/2_인스타용/...` 포함 확인
   - [ ] A/B/C 옵션 전환 시 각각 다른 텍스트 표시 확인

5. **경로 문제 발생 시**
   - [ ] 브라우저 개발자 도구(F12) → Console 탭 열기
   - [ ] 네트워크 탭에서 404 오류(빨간색) 확인
   - [ ] 오류 이미지 경로 확인 후 data.json에서 수정

### D. 일반적인 오류 및 해결법

| 증상 | 원인 | 해결법 |
|------|------|--------|
| **❌ 블로그 이미지만 엑박** | 경로가 `/photos/2_인스타용/...` (잘못된 폴더) | 경로를 `/photos/1_블로그용/...`으로 수정 |
| **❌ 인스타 이미지만 엑박** | 경로가 `/photos/1_블로그용/...` (잘못된 폴더) 또는 폴더 미생성 | 경로를 `/photos/2_인스타용/...`으로 수정 + 폴더 확인 |
| **❌ 공백 오류** | 폴더: `[작성완료] 260207_...` 인데 경로: `[작성완료]260207_...` (공백 없음) | 폴더명과 경로의 공백 일치 여부 확인 |
| **❌ 블로그 콘텐츠 안 뜸** | `content.A`가 객체 구조 (배열 아님) | `content.A`를 배열로 변경: `[{ "title": "...", "images": [...] }]` |
| **❌ 인스타 텍스트 안 뜸** | `content.A`가 배열 구조 (객체 아님) | `content.A`를 객체로 변경: `{ "text": "...", "audit": {...} }` |
| **❌ 전체 콘텐츠 안 뜸** | JSON 문법 오류 (쉼표, 따옴표, 괄호) | JSON Lint 검증 (https://jsonlint.com) |
| **⚠️ 교차 경로 오류** | 블로그 항목이 `2_인스타용` 참조 또는 인스타가 `1_블로그용` 참조 | **채널별로 정확한 경로 사용 반드시!** |
| **⚠️ ID-폴더명 불일치** | `id: "260207_조리기능장_수업"` 인데 폴더: `[작성완료] 260204_...` | ID와 폴더명의 날짜/주제 일치 확인 |

### E. 자동 검증 명령어 (선택)

#### PowerShell에서 교차 경로 검증
```powershell
# 로드
$json = Get-Content 'C:\Users\tahar\clawd\marketing_dashboard\public\data.json' -Raw | ConvertFrom-Json

# 블로그 항목에서 2_인스타용 경로 찾기 (절대 금지!)
Write-Host "=== 블로그 항목 검증 ===" -ForegroundColor Cyan
$hasError = $false
$json.blog | ForEach-Object {
  $id = $_.id
  $thumb = $_.thumbnail
  if ($thumb -like "*2_인스타용*") {
    Write-Host "❌ OOPS! 블로그[$id]가 2_인스타용 경로 사용: $thumb" -ForegroundColor Red
    $hasError = $true
  }
  foreach ($post in $_.content.A) {
    foreach ($img in $post.images) {
      if ($img -like "*2_인스타용*") {
        Write-Host "❌ OOPS! 블로그 이미지가 2_인스타용 경로: $img" -ForegroundColor Red
        $hasError = $true
      }
    }
  }
}
if (-not $hasError) { Write-Host "✅ 블로그: 모든 경로가 1_블로그용입니다" -ForegroundColor Green }

# 인스타 항목에서 1_블로그용 경로 찾기 (절대 금지!)
Write-Host "`n=== 인스타 항목 검증 ===" -ForegroundColor Cyan
$hasError = $false
$json.insta | ForEach-Object {
  $id = $_.id
  $thumb = $_.thumbnail
  if ($thumb -like "*1_블로그용*") {
    Write-Host "❌ OOPS! 인스타[$id]가 1_블로그용 경로 사용: $thumb" -ForegroundColor Red
    $hasError = $true
  }
}
if (-not $hasError) { Write-Host "✅ 인스타: 모든 경로가 2_인스타용입니다" -ForegroundColor Green }
```

#### 실행 방법
```powershell
# 위 코드를 PowerShell에 복사 후 실행
# 빨간 오류가 없으면 성공!
```


#### 폴더 존재 여부 확인
```powershell
# 블로그 폴더 확인
Write-Host "=== 블로그 폴더 ===" 
Get-ChildItem "C:\Users\tahar\clawd\marketing_dashboard\public\photos\1_블로그용" -Directory | Where-Object {$_.Name -like "*작성완료*"} | ForEach-Object {$_.Name}

# 인스타 폴더 확인
Write-Host "`n=== 인스타 폴더 ==="
Get-ChildItem "C:\Users\tahar\clawd\marketing_dashboard\public\photos\2_인스타용" -Directory | Where-Object {$_.Name -like "*작성완료*"} | ForEach-Object {$_.Name}
```

#### JSON 온라인 검증
- URL: https://jsonlint.com/
- 파일 복사 → 우측 editor에 붙여넣기 → Validate 클릭
- 빨간색 오류 표시 있으면 문법 오류 확인

---

## 6. 🚀 Vercel 배포 (최종 공개)

**[전제]** 아래 검증을 모두 통과한 뒤에만 배포합니다.
- `2.6 마무리 검증 가이드` 통과 ✅
- `5-B data.json 검증` (교차 경로 없음) 통과 ✅
- `5-C 대시보드 테스트` (A/B/C 전환, 404 없음) 통과 ✅

### 📍 배포 구조

| 항목 | 주소 |
| :--- | :--- |
| **GitHub 저장소** | https://github.com/ByoungOhBAE/academy-marketing-dashboard (**public**) |
| **Vercel 프로젝트** | https://vercel.com/byoungohbaes-projects/academy-marketing-dashboard |
| **운영 URL** | https://academy-marketing-dashboard.vercel.app |
| **배포 트리거** | `master` 브랜치 push → Vercel 자동 배포 |

**[중요]** 별도의 배포 명령이 없습니다. **`master`에 push하는 순간 공개 배포**됩니다.
push 전에 반드시 `git status`로 올라갈 파일을 눈으로 확인하세요.

### ⚠️ 사진 경로 이중 구조 (배포 시 가장 흔한 사고)

로컬과 Vercel은 사진을 **서로 다른 폴더에서** 읽습니다.

```
로컬 (server.js)  : /photos/... → C:\Users\tahar\clawd\academy_marketing\...
Vercel (정적 서빙): /photos/... → public/photos/...
```

➡️ **로컬에서 사진이 잘 보여도, `public/photos`에 복사가 안 되어 있으면 배포본에서는 엑박입니다.**
➡️ `2-D 폴더 이동/배포` 단계에서 `public/photos`로 복사했는지 반드시 재확인하세요.

### 1️⃣ 배포 전 최종 점검

```bash
cd C:\Users\tahar\clawd\marketing_dashboard
git status --short
```

- [ ] `public/data.json` 수정분이 있는가
- [ ] 신규 사진 폴더가 `public/photos/1_블로그용` 또는 `public/photos/2_인스타용` 아래에 있는가
- [ ] **의도하지 않은 파일이 섞여 있지 않은가** (이전 작업의 미커밋분이 함께 올라갈 수 있음)
- [ ] 공개 저장소이므로 **비공개 정보(수강생 개인정보, 내부 문서, 원본 스크린샷 등)가 없는가**

### 2️⃣ 커밋 & 푸시

```bash
cd C:\Users\tahar\clawd\marketing_dashboard
git add .
git commit -m "인스타 260804 학원수업 2건 등록"
git push origin master
```

또는 자동 스크립트 사용 (내부적으로 `git add .` → commit → push 수행):

```bash
node git_update.js "커밋 메시지"
```

### 3️⃣ 배포 후 검증 (필수)

푸시 후 약 30초~2분 뒤 아래를 확인합니다.

**A. data.json 반영 확인**

```bash
curl -s https://academy-marketing-dashboard.vercel.app/data.json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const d=JSON.parse(s);console.log('blog',d.blog.length,'/ insta',d.insta.length);console.log('최신 insta:',d.insta[0].id);console.log('최신 blog:',d.blog[0].id)})"
```

- [ ] 신규 항목이 배열 **맨 앞**에 보이는가

**B. 신규 이미지 200 응답 확인**

```bash
node -e "const u='https://academy-marketing-dashboard.vercel.app';fetch(u+'/data.json').then(r=>r.json()).then(async d=>{for(const it of [...d.blog.slice(0,2),...d.insta.slice(0,2)]){const r=await fetch(u+encodeURI(it.thumbnail));console.log(r.status,r.status===200?'OK':'❌ 404',it.id)}})"
```

- [ ] 전부 `200 OK` (하나라도 404면 `public/photos` 복사 누락)

**C. 브라우저 최종 확인** — https://academy-marketing-dashboard.vercel.app

- [ ] 블로그 모드: A/B/C 전환 시 각각 다른 내용 + 이미지 표시
- [ ] 인스타 모드: A/B/C 전환 시 각각 다른 내용 + 썸네일 표시
- [ ] F12 → Console/Network에 404 없음

### 4️⃣ 배포 실패·오류 대응

| 증상 | 원인 | 해결법 |
| :--- | :--- | :--- |
| **배포본만 이미지 엑박** | `public/photos`에 사진 폴더 미복사 | 폴더 복사 후 재커밋·푸시 |
| **data.json이 안 바뀜** | Vercel 빌드 대기 중 / 브라우저 캐시 | 1~2분 대기 후 강력 새로고침 (Ctrl+F5) |
| **push 거부 (rejected)** | 원격에 다른 커밋 존재 | `git pull origin master` 후 재푸시 |
| **로컬 서버 실행 안 됨 (EADDRINUSE)** | 3000 포트를 다른 프로젝트가 사용 중 | 해당 프로세스 종료 또는 다른 포트로 임시 실행 |

### 5️⃣ 용량 관리 주의

사진이 Git LFS 없이 저장소에 **직접** 저장됩니다. (현재 저장소 약 200MB)

- [ ] 업로드 전 사진 용량 확인 — 한 폴더가 수십 MB면 리사이즈 검토
- [ ] 인스타용 썸네일은 **긴 변 1080px 내외**면 충분
- [ ] 휴대폰 스크린샷은 상태바·SNS UI·내비게이션 바를 **크롭 후** 등록

**[참고]** 장기적으로는 `.gitignore` 주석대로 Git LFS 도입을 검토하세요.

---

## ✅ 전체 진행 순서 요약

```
1. 실시간 스캔 — [작성대기] 폴더 확인          (§1)
2. 사진 확인 — 주제 파악 + UI/불필요 영역 크롭   (§1, §6-5)
3. 원고 생성 — 채널별 A/B/C 3버전 + Footer      (§2-B)
4. data.json 저장 — 배열 맨 앞에 추가           (§2-C)
5. 폴더 배포 — public/photos로 복사             (§2-D)
6. 폴더명 변경 — [작성대기] → [작성완료]         (§2-E)
7. 로컬 검증 — localhost:3000 A/B/C + 404 확인  (§2.6, §5-C)
8. Vercel 배포 — master push                    (§6-2)
9. 배포 후 검증 — 운영 URL에서 재확인            (§6-3)
```
